import {createContext,useContext,useState} from 'react'; import {CheckCircle,XCircle} from 'lucide-react'; const C=createContext(); export function ToastProvider({children}){const [items,setItems]=useState([]); const push=(message,type='success')=>{const id=Date.now();setItems(x=>[...x,{id,message,type}]);setTimeout(()=>setItems(x=>x.filter(i=>i.id!==id)),3500)}; return <C.Provider value={push}>{children}<div className="fixed right-4 top-4 z-50 space-y-2">{items.map(i=><div key={i.id} className="card flex items-center gap-3 px-4 py-3"><span className={i.type==='error'?'text-red-600':'text-green-600'}>{i.type==='error'?<XCircle/>:<CheckCircle/>}</span><span className="text-sm font-medium">{i.message}</span></div>)}</div></C.Provider>}; export const useToast=()=>useContext(C);
