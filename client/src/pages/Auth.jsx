import {useState} from 'react'; import {Link,useNavigate} from 'react-router-dom'; import {Eye,EyeOff,FileText} from 'lucide-react'; import {useAuth} from '../context/AuthContext'; import {useToast} from '../components/Toast';
export function Login(){const {login}=useAuth(),toast=useToast(),nav=useNavigate();const [email,setEmail]=useState('applicant@passportdemo.com'),[password,setPassword]=useState('Applicant@123'),[show,setShow]=useState(false),[busy,setBusy]=useState(false);const submit=async e=>{e.preventDefault();setBusy(true);try{const u=await login(email,password);toast('Login successful');nav(u.role==='ADMIN'?'/admin':'/applicant')}catch(e){toast(e.response?.data?.message||'Login failed','error')}finally{setBusy(false)}};return <AuthBox title="Welcome back" sub="Sign in to your demo account"><form onSubmit={submit} className="space-y-4"><Field label="Email"><input className="input" value={email} onChange={e=>setEmail(e.target.value)} type="email" required/></Field><Field label="Password"><div className="relative"><input className="input pr-11" value={password} onChange={e=>setPassword(e.target.value)} type={show?'text':'password'} required/><button type="button" className="absolute right-3 top-3 text-slate-500" onClick={()=>setShow(!show)}>{show?<EyeOff size={18}/>:<Eye size={18}/>}</button></div></Field><button disabled={busy} className="btn-primary w-full">{busy?'Signing in…':'Sign in'}</button></form><div className="mt-5 text-center text-sm">New applicant? <Link className="text-blue-700 font-semibold" to="/register">Create account</Link></div><div className="mt-5 bg-slate-50 rounded-xl p-4 text-xs text-slate-600"><b>Demo:</b> applicant@passportdemo.com / Applicant@123<br/><b>Admin:</b> admin@passportdemo.com / Admin@123</div></AuthBox>}
export function Register(){const {register}=useAuth(),toast=useToast(),nav=useNavigate();const [form,setForm]=useState({name:'',email:'',password:'',phone:''}),[busy,setBusy]=useState(false);const submit=async e=>{e.preventDefault();setBusy(true);try{await register(form);toast('Account created');nav('/applicant')}catch(e){toast(e.response?.data?.message||'Registration failed','error')}finally{setBusy(false)}};return <AuthBox title="Create applicant account" sub="Use fictional information for this academic demo"><form onSubmit={submit} className="space-y-4"><Field label="Full name"><input className="input" required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></Field><Field label="Email"><input className="input" required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></Field><Field label="Phone"><input className="input" value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></Field><Field label="Password"><input className="input" required minLength="6" type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></Field><button disabled={busy} className="btn-primary w-full">{busy?'Creating…':'Create account'}</button></form><div className="mt-5 text-center text-sm">Already registered? <Link className="text-blue-700 font-semibold" to="/login">Sign in</Link></div></AuthBox>}
function Field({label,children}){return <div><label className="label">{label}</label>{children}</div>}function AuthBox({title,sub,children}){return <div className="min-h-[80vh] flex items-center justify-center px-5 py-10 bg-slate-50"><div className="w-full max-w-md"><div className="text-center mb-6"><div className="mx-auto h-12 w-12 rounded-2xl bg-navy text-white flex items-center justify-center"><FileText/></div><h1 className="text-2xl font-black text-navy mt-4">{title}</h1><p className="text-sm text-slate-500 mt-1">{sub}</p></div><div className="card p-6 md:p-8">{children}</div></div></div>}
